# DESIGN.md — 조수안 포트폴리오 디자인 시스템

> 이 문서는 `index.html`, `assets/style.css`, `assets/main.js`에 실제로 쓰인 값과 규칙을 정리한 것입니다.
> 새 섹션을 추가하거나 문구·색을 바꿀 때 이 문서의 토큰과 패턴을 따르면 기존 화면과 결이 맞습니다.

---

## 1. 컨셉

| 항목 | 내용 |
| --- | --- |
| 한 줄 정의 | "보이는 문제가 아닌, 진짜 원인을 짚는" 사람의 포트폴리오. 어두운 배경 위에 민트 한 색으로 핵심만 짚어낸다. |
| 원본 | 1920×1080 슬라이드 13장(PDF). 슬라이드의 좌·우 분할, 차콜 배경, 민트/그린 포인트를 웹에 그대로 옮겼다. |
| 톤 | 절제된 다크 UI. 움직임은 '등장할 때 한 번'만 쓰고, 계속 떠다니거나 돌아가는 장식은 두지 않는다. 색상은 무채색 + 민트 계열 두 가지만 쓴다. |
| 키워드 | 차콜, 민트, 정갈함, 데이터, 리듬감 |

---

## 2. 컬러 토큰

모두 `:root` CSS 변수로 정의되어 있으며 다크 단일 테마입니다(라이트 모드 없음).

### 배경

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--bg` | `#2B2B2B` | 기본 배경 (원본 슬라이드 배경색) |
| `--bg-deep` | `#1F1F1F` | 케이스·About의 왼쪽 패널, 로더, 마퀴 띠, 스크롤 후 네비 |
| `--bg-card` | `#343434` | Insight 인용 박스 |
| `--bg-card-2` | `#3A3A3A` | 예비 카드 톤 (현재 미사용) |
| `--line` | `rgba(255,255,255,.14)` | 구분선, 테두리 |

### 텍스트

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--text` | `#F4F4F4` | 본문·제목 기본 |
| `--text-dim` | `#C9C9C9` | 설명문, 보조 텍스트 |
| `--text-mute` | `#9A9A9A` | 라벨(SCROLL, CASE FILTER), 푸터 |
| `--ink` | `#232323` | 민트 배경 위에 올라가는 어두운 글자 |

### 포인트 (민트 → 그린 4단계)

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--mint-soft` | `#D9F5E8` | 성과 알약 카드 배경, Step 1 헤더 |
| `--mint` | `#8DE4BE` | **주 포인트색.** 제목 강조, 태그 배경, 라벨, 활성 네비, 스킬 바 |
| `--mint-mid` | `#67D9A5` | 그라디언트 중간값 (예비) |
| `--green` | `#2DB47C` | 커버의 큰 숫자, `em` 강조, Step 3 헤더, 화살표, CONTACT 라벨 |
| `--green-deep` | `#1F9D68` | 성과 알약 안의 굵은 숫자 (밝은 배경 위 대비 확보) |

### 색 사용 규칙

- 한 화면에서 민트 계열 강조는 **제목의 한 구절 + 라벨** 정도로 제한한다. 전부 민트면 아무것도 강조되지 않는다.
- 민트 배경(`--mint`, `--mint-soft`) 위의 글자는 항상 `--ink` 또는 `--green-deep`. 흰 글자를 올리지 않는다.
- 단계가 있는 요소(Step 1→2→3)는 `--mint-soft → --mint → --green` 순서로 진해진다. 2단계면 `--mint → --green`.
- 그라디언트는 `linear-gradient(90deg, var(--mint), var(--green))` 하나만 쓴다 (진행 바, 스킬 바, 로더 바).

---

## 3. 타이포그래피

### 폰트

```
--font: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont,
        "Apple SD Gothic Neo", "Noto Sans KR", "Malgun Gothic", system-ui, sans-serif;
```

- Pretendard Variable을 jsDelivr CDN(dynamic subset)으로 로드. 실패 시 시스템 한글 폰트로 자연스럽게 대체된다.
- 본문 `line-height: 1.6`, `letter-spacing: -0.01em`, `word-break: keep-all` (한글 단어 중간 줄바꿈 금지).

### 크기 스케일 (모두 `clamp()`로 유동)

| 역할 | 크기 | 굵기 | 자간 |
| --- | --- | --- | --- |
| 히어로 제목 | `clamp(34px, 6.4vw, 92px)` | 800 | -0.03em |
| 커버 제목 (BREADROP…) | `clamp(34px, 5.2vw, 80px)` | 800 | -0.03em |
| 커버 숫자 01/02/03 | `clamp(120px, 20vw, 330px)` | 800 | -0.05em, line-height .85 |
| 감사합니다! | `clamp(48px, 8vw, 120px)` | 800 | -0.04em |
| About 헤드라인 | `clamp(30px, 3.4vw, 52px)` | 500 (강조만 800) | -0.03em |
| 케이스 제목 | `clamp(26px, 2.8vw, 44px)` | 800 | -0.03em |
| 섹션 라벨(문제/전략/성과) | `clamp(18px, 1.5vw, 26px)` | 800 | -0.01em |
| Insight 인용 | `clamp(16px, 1.45vw, 24px)` | 700 | -0.02em |
| 성과 알약 본문 | `clamp(15px, 1.35vw, 22px)` | 400 (숫자 800) | — |
| 본문/설명 | `clamp(14px, 1.15vw, 18px)` | 400 | — |
| 소형 라벨 (SCROLL, CASE FILTER) | 11px | 600~700 | +0.2em, 대문자 |

### 타이포 규칙

- 제목은 800, 본문은 400. 중간 굵기(500~700)는 라벨과 인용에만 쓴다.
- 큰 글자는 자간을 음수로 조인다. 작은 라벨은 자간을 넓게 벌리고 대문자로 쓴다.
- 강조는 색으로: 한글 제목 안의 핵심 구절만 `--mint`(`strong`, `.accent`) 또는 `--green`(`em`).
- 영문 프로젝트명은 대문자 그대로(BREADROP, JOSEPH & STACEY, NAVER Z).

---

## 4. 레이아웃

### 그리드와 여백

| 토큰/규칙 | 값 |
| --- | --- |
| `--gutter` (좌우 여백) | `clamp(20px, 5vw, 80px)` |
| `--nav-h` | 64px (≤860px: 58px) |
| 최대 폭 | 히어로 1200px, 커버·연락처 1400px, 나머지는 전체 폭 |
| 섹션 세로 여백 | 70~120px |

### 좌·우 분할 패턴 (원본 슬라이드 계승)

원본 슬라이드는 왼쪽에 제목+이미지, 오른쪽에 내용이 있는 구조였다. 웹에서도 배경색을 두 톤으로 갈라 이 구조를 유지한다.

| 섹션 | 컬럼 비율 | 배경 분할선 |
| --- | --- | --- |
| About | 5 : 7 | 41.5% 지점에서 `--bg-deep` → `--bg` |
| Overview (BREADROP 개요) | 2 : 1 | 67% 지점에서 `--bg` → `--bg-deep` |
| Case (케이스 6개) | 39 : 61 | 39% 지점에서 `--bg-deep` → `--bg` |

구현: `background: linear-gradient(90deg, var(--bg-deep) 0 39%, var(--bg) 39%)`. 1100px 이하에서는 단일 컬럼 + 단색 배경으로 바뀐다.

### 페이지 구조 (스크롤 순서)

```
Loader (오버레이)
├─ Hero            #hero      p.1
├─ Marquee 띠
├─ About           #about     p.2
├─ Filter 바 (sticky)
├─ Cover 01        #project-1 p.3
│   ├─ Overview                p.4
│   ├─ Case B2B                p.5
│   ├─ Case B2C                p.6
│   └─ Case B2C                p.7
├─ Cover 02        #project-2 p.8
│   ├─ Case B2B                p.9
│   └─ Case B2C                p.10
├─ Cover 03        #project-3 p.11
│   └─ Case B2C                p.12
├─ Contact         #contact   p.13
└─ Footer
```

### z-index 레이어

| 레이어 | z | 요소 |
| --- | --- | --- |
| 로더 | 1000 | `.loader` |
| 토스트 | 960 | `.toast` |
| 라이트박스 | 950 | `.lightbox` |
| 스크롤 진행 바 | 900 | `.progress` |
| 네비게이션 | 800 | `.nav` |
| 섹션 도트 | 760 | `.dots` |
| 맨 위로 버튼 | 750 | `.totop` |
| 필터 바 (sticky) | 700 | `.filter` |

---

## 5. 컴포넌트

### 태그 `.tag`
민트 배경 + 잉크 글자, 직각 모서리(둥글기 없음). `#B2B`, `#B2C`에만 쓴다.
커버에서는 큰 사이즈, 케이스 상단에서는 `.tag--sm`(14px). 부모 `.reveal`과 함께 페이드로 등장.

### 키-밸류 `.kv`
`문제 / 목표 / 전략 / 성과` 라벨(민트, 800)과 내용을 좌우로 배치. 860px 이하에서는 위아래로.

### Step 흐름 `.steps`
- 화살표 모양 헤더(chevron)가 이어지는 3단(`.steps--3`) 또는 2단(`.steps--2`) 그리드.
- 헤더 배경은 단계별로 `--mint-soft → --mint → --green`, 본문 테두리도 같은 색.
- 헤더 높이는 `grid-template-rows: subgrid`로 맞춘다. 모바일에서는 세로 스택 + 화살표 제거.

### 캠페인 카드 `.campaign` / 흐름 카드 `.flow`
2열 카드. 첫 카드는 `--mint`, 둘째는 `--green`(`--deep` 변형). 안에 `기획 → 결과` 또는 `헤더 → 설명 → 화살표 → 결론`. 삼각형 화살표 `.arrow-down`은 112×36px의 납작한 역삼각형으로, 위쪽 변은 배경에 녹아들고(투명) 꼭짓점으로 갈수록 진해지는 세로 그라데이션. 첫 카드(밝은 계열)는 `--mint`, 둘째 카드와 Step 2(`--deep`)는 `--green`으로 끝난다.

### VS 비교표 `.vs`
한국 vs 일본처럼 두 컬럼을 나란히. 중앙에 `VS` 원형 배지(정적).

### 성과 알약 `.result`
- `--mint-soft` 배경, `border-radius: 999px`, 상단에 3D 아이콘이 반쯤 걸치게(`translate(-50%, -50%)`).
- 아이콘은 정적. 알약 카드가 `riseIn`으로 아래에서 올라오며 등장하고, 두 번째 카드는 0.15s 늦게.
- 숫자는 `--green-deep` 800. 카운트업 없이 최종값을 바로 보여준다.

### Insight 인용 `.insight`
`--bg-card` 박스, 라벨 "Insight"(민트), 큰따옴표 포함 인용문. 밑줄 강조 `u`는 배경 그라디언트가 0 → 100% 폭으로 그려지는 방식(1초, 0.4초 지연).

### 폰 목업 `.phones`
2열 그리드, 최대 560px. 이미지 자체에 프레임과 그림자가 포함되어 있으므로 CSS 테두리는 없다. 등장 시 오른쪽 폰이 0.12s 늦게 `riseIn`. 마우스 위치에 따라 `rotateY ±7°`, `rotateX ±5°` 틸트. 클릭 시 라이트박스.

### 네비게이션 `.nav`
투명 → 스크롤 20px 이후 `rgba(31,31,31,.82)` + blur 14px. 활성 링크는 민트 알약. 860px 이하에서 햄버거 메뉴.

### 필터 바 `.filter`
`ALL / #B2B / #B2C`. 네비 바로 아래에 sticky. 선택되지 않은 케이스는 `display: none`.

### 섹션 도트 `.dots`
화면 오른쪽 중앙, 6개 원. 활성은 민트 채움 + 1.35배 + 외곽 글로우. hover 시 섹션 이름이 왼쪽에 나타남. 1100px 이하 숨김.

### 기타
- **진행 바** `.progress`: 상단 3px, 스크롤 비율만큼 민트→그린 그라디언트.
- **마퀴** `.marquee`: 40초 주기 무한 좌측 흐름, hover 시 정지. 텍스트는 두 번 반복해 `translateX(-50%)`로 이어 붙임.
- **맨 위로** `.totop`: 600px 이상 스크롤 시 등장.
- **라이트박스**: 배경 `rgba(15,15,15,.92)` + blur. ESC·바깥 클릭으로 닫힘. 이미지 alt가 캡션이 된다.
- **토스트**: 복사 완료 알림, 1.8초 후 사라짐.

---

## 6. 모션 시스템

### 공통 이징

| 이름 | 값 | 용도 |
| --- | --- | --- |
| `--ease` | `cubic-bezier(.22, 1, .36, 1)` | 기본 등장·이동 (부드러운 감속) |
| 커튼 | `cubic-bezier(.76, 0, .24, 1)` | 로더 퇴장 |

### 지속 시간 기준

- 미세 반응(hover, 색 전환): 0.2~0.3s
- 요소 등장(reveal): 0.7~0.9s
- 로더 최소 노출: 1.7s
- 루프 애니메이션은 두 개뿐: 1.8s(SCROLL 인디케이터 선), 40s(마퀴)

### 등장 규칙 (`.reveal`)

1. 초기 상태 `opacity: 0; translateY(28px)`.
2. IntersectionObserver가 요소의 12%가 보이고 뷰포트 하단 8% 안쪽으로 들어오면 `.is-visible` 부여.
3. 한 번 보이면 다시 감추지 않는다(`unobserve`).
4. 같은 부모 안의 형제들은 0.12~0.18s씩 순차 지연(`nth-child` delay).

### 텍스트 마스크 등장 (`.fx-text`)

제목이 화면에 도착하면 단어들이 보이지 않는 마스크 뒤에서 위로 올라오며 나타난다. 글자가 바뀌거나 흔들리는 효과는 쓰지 않는다.

| 파라미터 | 값 |
| --- | --- |
| 단위 | 단어 (`span.w` > `span.in`) |
| 초기 상태 | `translateY(105%)`, 부모 `overflow: hidden` |
| 지속 시간 | 0.9s, `--ease` |
| 단어 간 지연 | 0.07s씩, 최대 0.6s |
| 마스크 여유 | `padding-bottom: .08em` (영문 디센더 잘림 방지) |

적용 대상: 커버 제목, 케이스 제목, About 헤드라인·이름·항목 라벨, 문제/전략/성과 라벨, 감사합니다!.
히어로 제목은 별도의 단어 단위 등장(`wordIn`, 0.09s 간격)을 쓴다.

구현 메모: JS가 텍스트 노드를 단어 단위로 감싼다. `<br>`, `<strong>`, `<em>`은 유지된다. 단어 단위로만 줄바꿈되므로 한글이 중간에 끊기지 않는다.

### 키프레임 목록

| 이름 | 효과 | 사용처 |
| --- | --- | --- |
| `loaderChar` | 아래에서 올라옴 | 로더 글자 |
| `loaderFill` | 폭 0 → 100% | 로더 진행 바 |
| `fadeIn` | 투명 → 불투명 | 로더 부제 |
| `wordIn` | 아래에서 올라옴 | 히어로 단어·태그 |
| `scrollLine` | 선을 훑는 민트 | SCROLL 인디케이터 |
| `marquee` | 좌측 무한 흐름 | 마퀴 |
| `riseIn` | 24px 아래에서 올라옴 | 폰 목업(오른쪽), 성과 알약 |

폰과 알약처럼 **인라인 transform을 나중에 덧쓰는 요소**는 `animation-fill-mode: backwards`를 쓴다. `forwards`를 쓰면 애니메이션 종료값이 hover 틸트를 영구히 덮어쓴다.

### 스크롤 연동

- **커버 숫자 패럴랙스**: 뷰포트 중앙 대비 위치(-0.5~0.5)에 `-50px`을 곱해 `--py`로 주입. rAF로 스로틀.
- **히어로 포인터 패럴랙스**: 마우스 위치에 따라 본문은 반대 방향 최대 8px, 빛은 같은 방향 최대 40px.
- **활성 섹션 판정**: 뷰포트 상단에서 35% 지점을 기준선으로, 이보다 위에 시작한 마지막 섹션을 활성으로 본다.

### 접근성

- `prefers-reduced-motion: reduce`이면 모든 애니메이션·전환을 0.001s로 만들고, 로더 최소 시간 0, 텍스트 등장은 즉시 완료(`.is-instant`), 패럴랙스·틸트 비활성.
- 라이트박스 대상 이미지는 `tabindex=0`, `role=button`, Enter/Space로 열림.
- 모든 목업 이미지에 내용을 설명하는 `alt`가 있고, 그 alt가 라이트박스 캡션으로 재사용된다.

---

## 7. 반응형 브레이크포인트

| 폭 | 변경 |
| --- | --- |
| ≤ 1100px | 좌·우 분할 → 단일 컬럼(배경 단색). 커버 숫자가 제목 아래로. 역할 원 3개가 가로 배치. 도트 네비 숨김. |
| ≤ 860px | 네비 → 햄버거 드롭다운, CTA 숨김. Step/캠페인/VS/성과가 1열 스택. `.kv` 세로 배치. 히어로 SCROLL 숨김. `--nav-h` 58px. |
| ≤ 480px | 히어로 제목 축소, 아바타 130px, 연락처 줄 wrap, 커버 숫자 120px 고정. |

가로 스크롤은 어느 폭에서도 생기지 않아야 한다(`body { overflow-x: hidden }` + 모든 이미지 `max-width: 100%`).

---

## 8. 이미지 규격

| 파일 | 규격 | 비고 |
| --- | --- | --- |
| `p*-L.webp`, `p*-R.webp` (폰 목업 12장) | 640×1283, WebP q84 | 프레임·그림자 포함. 원본 슬라이드에서 2.5배로 렌더 후 크롭 |
| `laptop.webp` | 1600×970, WebP q86, 투명 배경 | 노트북 프레임 포함, 그림자·테두리 없음 |
| `icon-*.webp` (11개) | 200×200, 원형 알파 마스크 | 민트 원 배경 포함 |
| `avatar.webp` | 360×360, 원형 알파 마스크 | |
| `portfolio.pdf` | 원본 | "PDF 원본 보기" 링크 |

새 스크린샷을 추가할 때는 같은 이름 규칙(`p{프로젝트}-{케이스}-{L|R}.webp`)과 640px 폭을 지킨다.

---

## 9. 새 케이스를 추가할 때 체크리스트

1. `<article class="case section" data-type="b2b|b2c">` 로 시작. 필터가 `data-type`을 읽는다.
2. 왼쪽: `.tag--sm` → `.case__title.reveal.fx-text` → `.case__desc.reveal` → `.phones.reveal` (이미지 2장).
3. 오른쪽: `.kv`(문제/목표) → 전략(`.steps` / `.campaigns` / `.flows` / `.vs` 중 하나) → `.results` → `.insight`.
4. 숫자는 `<b>`로 감싸 최종값을 그대로 쓴다(카운트업 없음).
5. 라벨(`문제`, `전략`, `성과`)에는 `fx-text`를 붙인다.
6. 이미지 `alt`를 캡션처럼 쓴다.
7. 400px과 1440px에서 스크린샷으로 확인한다.
